# --- Generic ---
__pycache__/
*.py[cod]
*.swp
*.swo
.pytest_cache/
.DS_Store

# --- Local env and virtualenvs ---
.env
venv/
.venv/

<<<<<<< HEAD
# Crystal GUI runtime and generated outputs
data/results/
data/uploads/
data/dataset_uploads/
data/preprocessed/
data/generated_batch_ui_test/
data/synth_jobs/

# --- Keep folder structure under data/, ignore all contents ---
data/**               # ignore everything under data/
!data/**/             # but re-allow directories (so we can place exceptions inside)
!data/**/.gitkeep     # keep .gitkeep placeholders anywhere under data/
=======
# --- data: ignore everything by default ---
data/**

# Re-allow directories so we can keep placeholders
!data/**/
# Keep .gitkeep placeholders anywhere
!data/**/.gitkeep

# Keep ONLY the canonical preset JSONs
!data/synth_presets/[Ss]tandard.json
!data/preproc_presets/[Ss]tandard.json
>>>>>>> 6a75b7f (Clean snapshot: keep data/ tree; ignore contents; track only Standard presets + code updates)
